// Selecciona el formulario por su ID
const formulario = document.getElementById("localAccountForm");

console.log("fui selecciona el formulario");

// Agrega el atributo 'novalidate' al formulario
formulario.setAttribute("novalidate", "true");
